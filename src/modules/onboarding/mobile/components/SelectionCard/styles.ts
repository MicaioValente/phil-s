import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    flex: 1,
    height: 50,
    borderRadius: 15,
    borderWidth: 2,
    borderColor: '#333',
  },
  text: {
    color: '#fff',
    fontWeight: '700',
    fontSize: 12,
  },
});

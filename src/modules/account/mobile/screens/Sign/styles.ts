import { styled } from 'tamagui';
import { AntDesign, MaterialIcons, Zocial } from '@expo/vector-icons';

export const GoogleIcon = styled(AntDesign, {
  color: '$black1',
});

export const AppleIcon = styled(AntDesign, {
  color: '$black1',
});

export const EmailIcon = styled(Zocial, {
  color: '$black1',
});

export const PhoneIcon = styled(MaterialIcons, {
  color: '$black1',
});

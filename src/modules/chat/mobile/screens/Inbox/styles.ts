import { styled } from 'tamagui';
import { AntDesign, MaterialIcons } from '@expo/vector-icons';

export const ArrowLeft = styled(AntDesign, {
  color: '#FFF',
});

export const Plus = styled(AntDesign, {
  color: '#FFF',
});

export const MessageIcon = styled(MaterialIcons, {});

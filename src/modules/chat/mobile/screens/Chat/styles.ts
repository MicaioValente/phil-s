import { styled } from 'tamagui';
import { Ionicons, AntDesign } from '@expo/vector-icons';

export const VideoIcon = styled(Ionicons, {
  color: '#242424',
});

export const CallIcon = styled(Ionicons, {
  color: '#242424',
});

export const ArrowLeft = styled(AntDesign, {
  color: '#FFF',
});

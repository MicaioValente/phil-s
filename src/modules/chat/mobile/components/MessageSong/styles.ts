import { styled } from 'tamagui';
import { Ionicons, AntDesign, Zocial} from '@expo/vector-icons';

export const LockIcon = styled(Ionicons, {
  color: '#fff',
});

export const MusicalIcon = styled(Ionicons, {
  color: '#BBBBBB',
});


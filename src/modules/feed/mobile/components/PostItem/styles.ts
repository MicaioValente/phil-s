import {Entypo, Fontisto} from '@expo/vector-icons';
import { styled } from 'tamagui';

export const DotsThreeIcon = styled(Entypo, {
    position: 'absolute',
    right: 0,
    bottom: 0,
})

export const FavoriteIcon = styled(Fontisto, {
    color: '#8C8C8C',
})